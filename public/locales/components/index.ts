import { counter } from './counter.component';
import { header } from './header.component';
import { hello } from './hello.component';

export const langComponents = {
  header,
  hello,
  counter,
};
